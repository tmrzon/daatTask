import { FilerByTitleYearPipe } from './filer-by-title-year.pipe';

describe('FilerByTitleYearPipe', () => {
  it('create an instance', () => {
    const pipe = new FilerByTitleYearPipe();
    expect(pipe).toBeTruthy();
  });
});
